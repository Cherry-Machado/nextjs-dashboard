# Set default behavior, in case users don't have core.autocrlf set.
# This will handle line endings automatically for files Git considers text.
* text=auto

# Explicitly declare text files to ensure they are normalized.
# These will be checked out with platform-specific line endings, but committed with LF.
*.css text
*.html text
*.js text
*.json text
*.jsx text
*.md text
*.ts text
*.tsx text
*.yaml text
*.yml text

# Declare files that should always have LF line endings.
*.sh eol=lf